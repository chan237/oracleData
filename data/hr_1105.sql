select * FROM employees where department_id = 110;
SELECT  EMPLOYEE_ID, FIRST_NAME, SALARY FROM EMPLOYEES 
WHERE SALARY < 5000;
select * FROM employees;
SELECT EMPLOYEE_ID, JOB_ID FROM EMPLOYEES WHERE upper(FIRST_NAME) = 'JOHN';
SELECT * FROM EMPLOYEES WHERE SALARY >= 5000 AND SALARY <=10000;
SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 134 OR EMPLOYEE_ID = 201 or EMPLOYEE_ID = 107;
SELECT * FROM EMPLOYEES WHERE NOT JOB_ID = 'FI_MGR';
SELECT EMPLOYEE_ID, FIRST_NAME, SALARY FROM EMPLOYEES 
WHERE SALARY >= 2500 AND SALARY <=4500;
SELECT EMPLOYEE_ID, FIRST_NAME, SALARY FROM EMPLOYEES 
WHERE SALARY BETWEEN 2500 AND 4500;
SELECT EMPLOYEE_ID, FIRST_NAME, commission_pct FROM EMPLOYEES 
WHERE commission_pct = 0.3 or commission_pct = 0.05 or commission_pct = 0.1;
SELECT EMPLOYEE_ID, FIRST_NAME, commission_pct FROM EMPLOYEES 
WHERE commission_pct in(0.3,0.05,0.1);
SELECT EMPLOYEE_ID, FIRST_NAME FROM EMPLOYEES WHERE FIRST_NAME NOT LIKE '%a%';
SELECT FIRST_NAME, LAST_NAME, JOB_ID, EMPLOYEE_ID FROM EMPLOYEES WHERE MANAGER_ID IS NULL;
SELECT EMPLOYEE_ID, FIRST_NAME, SALARY FROM EMPLOYEES ORDER BY SALARY DESC;
SELECT EMPLOYEE_ID, FIRST_NAME, HIRE_DATE FROM EMPLOYEES ORDER BY HIRE_DATE DESC;
SELECT EMPLOYEE_ID, FIRST_NAME, SALARY FROM EMPLOYEES WHERE LENGTH(FIRST_NAME)=6; 
SELECT upper(FIRST_NAME), SALARY, COMMISSION_PCT FROM EMPLOYEES WHERE INITCAP(EMAIL)=INITCAP('jking');
SELECT * FROM EMPLOYEES WHERE SUBSTR(HIRE_DATE,1,2) = '03';
SELECT * FROM EMPLOYEES WHERE FIRST_NAME LIKE '%k';
SELECT EMPLOYEE_ID, FIRST_NAME, JOB_ID FROM EMPLOYEES WHERE MOD(EMPLOYEE_ID,2)=0;
SELECT EMPLOYEE_ID, FIRST_NAME, SALARY, MANAGER_ID FROM EMPLOYEES WHERE MANAGER_ID is null;
SELECT EMPLOYEE_ID, 
       FIRST_NAME, 
       NVL(MANAGER_ID, 0) AS "MANAGER_ID"       
FROM EMPLOYEES
WHERE MANAGER_ID IS NULL ;
ALTER TABLE EMP01 ADD(CREDATE,DATE);